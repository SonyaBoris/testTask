export type TData = {
  name: string,
  tel: string,
  agreement: boolean,
}

export type TCard = {
  id: number,
  image: string,
  text: string,
  desc: string,
}

export type TQuestion = {
  id: number,
  text: string,
  desc: string,
}

export type TUser = {
  id: number,
  name: string,
  location: string,
  comment: string
}